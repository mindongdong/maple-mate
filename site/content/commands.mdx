---
title: 명령어
description: 메이트의 모든 슬래시 명령어를 6가지 그룹으로 정리했습니다. 각 명령에는 전제조건 배지(등록 없이 / 캐릭터 등록 / API 키 등록)가 붙어요.
---

import { CommandGroup } from '@/components/CommandGroup'
import { BadgeLegend } from '@/components/Badge'

# 명령어

메이트의 모든 슬래시 명령어입니다. 각 명령에는 **전제조건 배지**가 붙어요 — 디스코드 서버에 초대만 하면 바로 쓰는 명령부터, 캐릭터 등록이나 넥슨 API 키가 필요한 명령까지. 인자·사용법은 디스코드에서 <code>/</code> 를 입력하면 그대로 표시됩니다.

<BadgeLegend />

## 등록·관리

<CommandGroup id="register" />

## 스펙·장비

<CommandGroup id="spec" />

## 이력

<CommandGroup id="history" />

## 리더보드

<CommandGroup id="leaderboard" />

## 스케줄러 숙제

<CommandGroup id="scheduler" />

## 알림 설정

<CommandGroup id="notify" />
